[build]
  command = "npm run build"
  publish = "dist"   # Vite default; pokud máš jinam, uprav

[[edge_functions]]
  function = "proxy"
  path = "/proxy"

# (volitelné, ale běžné pro SPA)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200